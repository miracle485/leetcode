package leetcode;

import java.util.*;

public class LeetCode1001 {
    public int[] gridIllumination(int n, int[][] lamps, int[][] queries) {
        Map<Integer, Integer> pdiagonal = new HashMap<>();
        Map<Integer, Integer> adiagonal = new HashMap<>();
        Map<Integer, Integer> row = new HashMap<>();
        Map<Integer, Integer> cow = new HashMap<>();
        Set<Long> light=new HashSet<>(30000);
        return null;
    }
}
